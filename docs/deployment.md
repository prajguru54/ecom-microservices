# Deployment

Phase 1 supports local single-node infrastructure through Docker Compose.

Use:

- `scripts/dev-up.sh` to start infra
- `scripts/dev-down.sh` to stop infra
- `scripts/migrate-all.sh` once services and migrations are in place
